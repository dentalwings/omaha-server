build:
  environment:
    python: 3.7.1
  nodes:
    tests:
      dependencies:
        before:
          - sudo apt-get update

filter:
  dependency_paths:
    - "omaha_server/assets/components/bower_components/"
  excluded_paths:
    - "*/migrations/*"
